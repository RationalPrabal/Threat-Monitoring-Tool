# Django Settings
DEBUG=True
SECRET_KEY=generate_a_secure_secret_key_here
ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
DJANGO_SETTINGS_MODULE=config.settings.local

# Database (PostgreSQL) - Optional for local dev (SQLite used by default if not set)
# DATABASE_URL=postgres://user:password@localhost:5432/threatmonitor

# JWT Configuration
JWT_SECRET_KEY=generate_another_secure_key_for_jwt

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Docker Database Settings (Only needed for Docker Compose)
POSTGRES_DB=threatmonitor
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
